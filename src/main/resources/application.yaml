server:
  port: 8080
  servlet:
    context-path: /shipping

spring:
  datasource:
    url: "jdbc:mysql://localhost:3306/shipping"
    username: root
    password: root
  jpa:
    hibernate:
      ddl-auto: update
    show-sql: true